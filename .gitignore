node_modules
.svelte-kit
build
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# macOS
.DS_Store

# Claude / Superpowers / Python venv
.claude/
.superpowers/
.venv/

# Per-algorithm step data — hosted on Hugging Face Datasets, not committed.
# Add the folder path here when introducing a new algorithm.
/static/dapo/
/static/drgrpo/
/static/grpo/
/static/legacy/
/static/ppo/
/static/reinforce/

# Local-only mock / rollout samples (not shipped).
/static/*_mock.json
/static/grpo-rollout.json
/static/grpo_mock/

# Local-only working folders (raw data, design assets, meeting notes, papers, legacy artifacts).
/data/
/data-spec/
/design/
/legacy/
/meeting/
/paper/

# docs/ is tracked (public docs like the schema). Only superpowers work artifacts are excluded.
/docs/superpowers/

# Local working notes (Claude instructions, roadmap).
/CLAUDE.md
/plan.md

# Dev-only scripts. Public scripts like build_data.py remain tracked.
/scripts/dev_*.py
